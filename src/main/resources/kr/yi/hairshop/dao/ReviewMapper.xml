<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.yi.hairshop.dao.ReviewMapper">

	<select id="selectReview" resultType="Review">
		select r_no, r_title, r_writer, r_writetime from review where r_delete=0 order by r_no desc
	</select>
	
	<insert id="insertReview" parameterType="Review" >
		INSERT INTO review
		(r_title, r_writer, r_writetime, r_content, r_file, r_g_no)
		VALUES(#{rTitle}, #{rWriter}, now(), #{rContent}, #{rFile}, #{rGNo.gNo})
	</insert>

	<update id="deleteReview">
		update review
		set r_delete = true
		where r_no=#{rNo}
	</update>

	<select id="selectListByNo" resultType="Review">
		select r_no, r_title, r_writer, r_writetime, r_content, r_file, r_g_no as rGNo from review where r_no=#{rNo}
	</select>

	<update id="updateReview" parameterType="Review">
		UPDATE review
		SET r_title=#{rTitle}, r_writetime=now(), r_content=#{rContent}, r_file=#{rFile}
		WHERE r_no=#{rNo}
	</update>

</mapper>